{
  "name": "seiyu-yourrepo",
  "version": "1.0.0",
  "description": "Automatic fill the questionnaire for me",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf dist/*",
    "build": "npm run clean && tsc",
    "start": "npm run build && node .",
    "debug": "npm run build && node --inspect-brk=9229 ."
  },
  "author": "xingo",
  "license": "MIT",
  "dependencies": {
    "date-fns": "^2.0.0-beta.5",
    "puppeteer-core": "^1.19.0",
    "qrcode": "^1.4.1",
    "qrcode-terminal": "^0.12.0",
    "typescript": "^3.5.3"
  },
  "devDependencies": {
    "@types/node": "^12.7.2",
    "@types/puppeteer-core": "^1.9.0",
    "@types/qrcode": "^1.3.4"
  }
}
